import { Link } from '@tanstack/react-router'
import { AnimatePresence, motion } from 'framer-motion'
import { ChevronDown, ChevronLeft, ChevronRight, FileText, Filter, Grid3X3, Image, List, Plus, Search, Trash2 } from '../components/heroicons'
import { useEffect, useMemo, useRef, useState } from 'react'
import { api, displayDate } from '../api'
import { Button, Card, EmptyState, Input, Select } from '../components/ui/index'
import type { Project, Report } from '../types'

type SortOption = 'date-desc' | 'date-asc' | 'title-asc' | 'title-desc' | 'updated-desc'
type ViewMode = 'grid' | 'list'

function PaginationNumbers({ currentPage, pageCount, onChange }: { currentPage: number; pageCount: number; onChange: (page: number) => void }) {
  const items: (number | 'ellipsis')[] = pageCount <= 7 ? Array.from({ length: pageCount }, (_, index) => index + 1) : (() => {
    const pages = new Set([1, pageCount, currentPage, currentPage - 1, currentPage + 1])
    const ordered = [...pages].filter((page) => page > 0 && page <= pageCount).sort((a, b) => a - b)
    return ordered.flatMap((page, index) => index && page - ordered[index - 1] > 1 ? ['ellipsis', page] : [page])
  })()
  return <div className="flex items-center gap-1">{items.map((item, index) => item === 'ellipsis' ? <span key={`ellipsis-${index}`} className="px-1 text-[8px] text-[#8a9188]">…</span> : <button type="button" key={item} onClick={() => onChange(item)} aria-current={item === currentPage ? 'page' : undefined} className={`grid h-9 min-w-9 place-items-center rounded-lg px-2 text-[8px] font-medium transition ${item === currentPage ? 'bg-[#e8f3de] text-[#49633f]' : 'text-[#737b70] hover:bg-[#eef1eb]'}`}>{item}</button>)}</div>
}

export function Reports() {
  const [reports, setReports] = useState<Report[]>([]), [projects, setProjects] = useState<Project[]>([]), [projectId, setProjectId] = useState(''), [query, setQuery] = useState(''), [status, setStatus] = useState(''), [sort, setSort] = useState<SortOption>('date-desc'), [view, setView] = useState<ViewMode>('grid'), [filterOpen, setFilterOpen] = useState(false), [deletingReport, setDeletingReport] = useState<Report | null>(null), [pageSize, setPageSize] = useState(15), [page, setPage] = useState(1), [busy, setBusy] = useState(false)
  const searchRef = useRef<HTMLInputElement>(null)
  const load = () => api<Report[]>('/reports?summary=1').then(setReports)
  useEffect(() => { void load(); void api<Project[]>('/projects').then(setProjects) }, [])
  useEffect(() => { const onKeyDown = (event: KeyboardEvent) => { if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === 'k') { event.preventDefault(); searchRef.current?.focus() } }; window.addEventListener('keydown', onKeyDown); return () => window.removeEventListener('keydown', onKeyDown) }, [])
  useEffect(() => { setPage(1) }, [query, projectId, status, sort, pageSize])
  const filtered = useMemo(() => {
    const normalizedQuery = query.trim().toLocaleLowerCase('id-ID')
    return reports
      .filter((report) => (!projectId || report.project_id === Number(projectId)) && (!status || report.status === status) && `${report.title} ${report.content} ${report.tags.join(' ')}`.toLocaleLowerCase('id-ID').includes(normalizedQuery))
      .sort((a, b) => {
        if (sort === 'title-asc') return a.title.localeCompare(b.title, 'id')
        if (sort === 'title-desc') return b.title.localeCompare(a.title, 'id')
        if (sort === 'date-asc') return a.report_date.localeCompare(b.report_date) || a.id - b.id
        if (sort === 'updated-desc') return b.updated_at.localeCompare(a.updated_at)
        return b.report_date.localeCompare(a.report_date) || b.id - a.id
      })
  }, [reports, query, projectId, status, sort])
  const pageCount = Math.max(1, Math.ceil(filtered.length / pageSize))
  const currentPage = Math.min(page, pageCount)
  const visibleReports = filtered.slice((currentPage - 1) * pageSize, currentPage * pageSize)
  const firstResult = filtered.length ? (currentPage - 1) * pageSize + 1 : 0
  const lastResult = Math.min(currentPage * pageSize, filtered.length)
  const paginationItems = useMemo<(number | 'ellipsis')[]>(() => {
    if (pageCount <= 7) return Array.from({ length: pageCount }, (_, index) => index + 1)
    const pages = new Set([1, pageCount, currentPage, currentPage - 1, currentPage + 1])
    const ordered = [...pages].filter((item) => item > 0 && item <= pageCount).sort((a, b) => a - b)
    return ordered.flatMap((item, index) => index && item - ordered[index - 1] > 1 ? ['ellipsis', item] : [item])
  }, [currentPage, pageCount])
  async function remove(id: number) { setBusy(true); try { await api(`/reports/${id}`, { method: 'DELETE' }); setDeletingReport(null); await load() } finally { setBusy(false) } }
  return <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }}>{deletingReport && <div className="fixed inset-0 z-50 grid place-items-center bg-[#20251f]/45 p-4 backdrop-blur-sm"><Card className="w-full max-w-md p-6"><div className="mb-4 grid h-11 w-11 place-items-center rounded-xl bg-[#fff0ed] text-[#b23c27]"><Trash2 size={19} /></div><h2 className="font-display text-lg font-medium">Hapus laporan?</h2><p className="mt-2 text-sm leading-6 text-[#747b71]">“{deletingReport.title}” dan seluruh lampirannya akan dihapus permanen. Tindakan ini tidak dapat dibatalkan.</p><div className="mt-6 flex justify-end gap-2"><Button $variant="secondary" disabled={busy} onClick={() => setDeletingReport(null)}>Batal</Button><Button $variant="danger" disabled={busy} onClick={() => void remove(deletingReport.id)}>{busy ? 'Menghapus…' : 'Hapus laporan'}</Button></div></Card></div>}<div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="mb-2 text-xs font-medium uppercase tracking-[.15em] text-[#789168]">Koleksi</p><h1 className="font-display text-3xl font-medium tracking-[-.04em]">Laporan harian</h1><p className="mt-2 text-sm text-[#777d74]">Satu hari boleh memiliki beberapa laporan.</p></div><Link to="/reports/new"><Button><Plus size={17} /> Tambah laporan</Button></Link></div>
    <div className="relative z-20 mb-5 p-0"><div className="flex flex-col gap-3 md:flex-row md:items-center"><div className="relative min-w-0 flex-1"><Search size={16} className="absolute left-3.5 top-3.5 text-[#92988f]" /><Input ref={searchRef} value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Cari judul, isi, atau tag…" className="!pr-20 !pl-10" /><kbd className="pointer-events-none absolute right-3 top-2.5 hidden rounded-md border border-[#e0e3dc] bg-[#f7f8f5] px-2 py-1 text-[10px] font-medium text-[#8b9188] sm:block">⌘ K</kbd></div><div className="flex items-center gap-2"><div className="relative"><button type="button" onClick={() => setFilterOpen(!filterOpen)} aria-expanded={filterOpen} aria-haspopup="menu" className={`flex h-11 items-center gap-2 rounded-xl border px-3 text-sm font-medium transition ${filterOpen || projectId || status || sort !== 'date-desc' ? 'border-[#b9cfac] bg-[#eef5e9] text-[#49633f]' : 'border-[#dfe2da] bg-white text-[#4f574c]'}`}><Filter size={16} /> Filter {(projectId || status || sort !== 'date-desc') && <span className="grid h-5 min-w-5 place-items-center rounded-full bg-[#6c8f58] px-1 text-[10px] text-white">{Number(Boolean(projectId)) + Number(Boolean(status)) + Number(sort !== 'date-desc')}</span>}<ChevronDown size={15} className={`transition-transform ${filterOpen ? 'rotate-180' : ''}`} /></button>{filterOpen && <div role="menu" className="absolute right-0 top-12 w-72 rounded-2xl border border-[#e1e4dc] bg-white p-4 shadow-xl"><div className="mb-3 flex items-center justify-between"><p className="text-xs font-medium uppercase tracking-[.12em] text-[#858b82]">Filter laporan</p><button type="button" onClick={() => { setProjectId(''); setStatus(''); setSort('date-desc') }} className="text-xs font-medium text-[#668457]">Reset</button></div><div className="space-y-3"><label className="block text-xs font-medium text-[#545c50]">Project<Select value={projectId} onChange={(e) => setProjectId(e.target.value)} aria-label="Filter project"><option value="">Semua project</option>{projects.map((project) => <option key={project.id} value={project.id}>{project.name}</option>)}</Select></label><label className="block text-xs font-medium text-[#545c50]">Status<Select value={status} onChange={(e) => setStatus(e.target.value)} aria-label="Filter status"><option value="">Semua status</option><option value="published">Final</option><option value="draft">Draf</option></Select></label><label className="block text-xs font-medium text-[#545c50]">Urutkan<Select value={sort} onChange={(e) => setSort(e.target.value as SortOption)} aria-label="Urutkan laporan"><option value="date-desc">Terbaru dulu</option><option value="date-asc">Terlama dulu</option><option value="updated-desc">Terakhir diubah</option><option value="title-asc">Judul A–Z</option><option value="title-desc">Judul Z–A</option></Select></label></div></div>}</div><div className="flex rounded-xl bg-[#f1f3ee] p-1" role="group" aria-label="Mode tampilan"><button type="button" onClick={() => setView('grid')} aria-label="Tampilan grid" aria-pressed={view === 'grid'} className={`flex h-9 items-center gap-2 rounded-lg px-3 text-xs font-medium transition ${view === 'grid' ? 'bg-white text-[#293126] shadow-sm' : 'text-[#7b8277] hover:text-[#293126]'}`}><Grid3X3 size={15} /> Grid</button><button type="button" onClick={() => setView('list')} aria-label="Tampilan list" aria-pressed={view === 'list'} className={`flex h-9 items-center gap-2 rounded-lg px-3 text-xs font-medium transition ${view === 'list' ? 'bg-white text-[#293126] shadow-sm' : 'text-[#7b8277] hover:text-[#293126]'}`}><List size={16} /> List</button></div></div></div></div>
    {filtered.length ? <><div className={view === 'grid' ? 'grid gap-5 md:grid-cols-2 xl:grid-cols-3' : 'grid gap-2'}><AnimatePresence initial={false} mode="popLayout">{visibleReports.map((report) => view === 'grid' ? <Card layout initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ layout: { duration: .38, ease: [0.22, 1, 0.36, 1] }, opacity: { duration: .2 }, y: { duration: .28, ease: 'easeOut' } }} key={report.id} style={{ borderColor: `${report.project_color}55` }} className="group p-5 transition-[transform,box-shadow,border-color] duration-300 hover:-translate-y-1 hover:shadow-lg"><div className="mb-5 flex items-start justify-between"><div className="grid h-10 w-10 place-items-center rounded-xl text-white" style={{ backgroundColor: report.project_color }}><FileText size={18} /></div><button disabled={busy} onClick={() => setDeletingReport(report)} aria-label={`Hapus ${report.title}`} className="rounded-lg p-2 text-[#a2a69f] opacity-0 transition hover:bg-red-50 hover:text-red-600 group-hover:opacity-100 group-focus-within:opacity-100"><Trash2 size={15} /></button></div><Link to="/reports/$reportId" params={{ reportId: String(report.id) }}><div className="mb-2 flex items-center gap-2"><span className="h-2 w-2 rounded-full" style={{ background: report.project_color }} /><span className="truncate text-[10px] font-medium uppercase tracking-[.1em] text-[#858b82]">{report.project_name}</span>{report.sync_status !== 'manual' && <span className="ml-auto rounded-full bg-[#e9f1e4] px-2 py-0.5 text-[9px] font-medium text-[#647b58]">{report.sync_status === 'synced' ? 'SYNC' : 'DIUBAH'}</span>}</div><h2 className="font-display truncate font-medium">{report.title}</h2><p className="mt-2 line-clamp-2 min-h-10 text-sm leading-5 text-[#747a71]">{report.content.replace(/[#*_`]/g, '')}</p><div className="mt-5 flex items-center justify-between border-t border-[#eef0ea] pt-4"><span className="text-xs font-medium text-[#868c83]">{displayDate(report.report_date)}</span><span className="flex items-center gap-1.5 text-xs text-[#868c83]"><Image size={14} /> {report.attachments.length}</span></div></Link></Card> : <Card layout initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ layout: { duration: .38, ease: [0.22, 1, 0.36, 1] }, opacity: { duration: .2 }, y: { duration: .28, ease: 'easeOut' } }} key={report.id} className="group flex items-center transition-[transform,box-shadow] duration-300 hover:-translate-y-0.5 hover:shadow-md gap-3 p-3 sm:gap-4 sm:p-4"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl text-white" style={{ backgroundColor: report.project_color }}><FileText size={18} /></div><Link to="/reports/$reportId" params={{ reportId: String(report.id) }} className="min-w-0 flex-1 sm:grid sm:grid-cols-[minmax(180px,1fr)_minmax(120px,180px)_110px_70px] sm:items-center sm:gap-4"><div className="min-w-0"><div className="mb-1 flex items-center gap-2"><span className="h-2 w-2 shrink-0 rounded-full" style={{ background: report.project_color }} /><span className="truncate text-[10px] font-medium uppercase tracking-[.1em] text-[#858b82]">{report.project_name}</span></div><h2 className="truncate font-display text-sm font-medium">{report.title}</h2></div><p className="mt-1 truncate text-xs text-[#747a71] sm:mt-0">{report.content.replace(/[#*_`]/g, '')}</p><span className="mt-1 block text-xs font-medium text-[#868c83] sm:mt-0">{displayDate(report.report_date)}</span><span className="mt-1 flex items-center gap-1.5 text-xs text-[#868c83] sm:mt-0"><Image size={14} /> {report.attachments.length}</span></Link><button disabled={busy} onClick={() => setDeletingReport(report)} aria-label={`Hapus ${report.title}`} className="shrink-0 rounded-lg p-2 text-[#a2a69f] opacity-100 transition hover:bg-red-50 hover:text-red-600 sm:opacity-0 sm:group-hover:opacity-100 sm:group-focus-within:opacity-100"><Trash2 size={15} /></button></Card>)}</AnimatePresence></div><div className="mt-5 flex flex-col items-center justify-between gap-3 px-1 py-3 sm:flex-row"><div className="flex items-center gap-3 text-xs text-[#747a71]"><span>Menampilkan {firstResult}–{lastResult} dari {filtered.length}</span><label className="flex items-center gap-2"><span>Per halaman</span><Select compact dropUp value={pageSize} onChange={(e) => setPageSize(Number(e.target.value))} className="w-16"><option value="15">15</option><option value="30">30</option><option value="60">60</option><option value="120">120</option></Select></label></div><div className="flex items-center gap-2"><Button className="!h-9 !w-9 !p-0" $variant="secondary" disabled={currentPage === 1} onClick={() => setPage(currentPage - 1)} aria-label="Halaman sebelumnya"><ChevronLeft size={16} /></Button><PaginationNumbers currentPage={currentPage} pageCount={pageCount} onChange={setPage} /><Button className="!h-9 !w-9 !p-0" $variant="secondary" disabled={currentPage === pageCount} onClick={() => setPage(currentPage + 1)} aria-label="Halaman berikutnya"><ChevronRight size={16} /></Button></div></div></> : <EmptyState icon={<FileText size={20} />} title={reports.length ? 'Tidak ditemukan' : 'Belum ada laporan'} body={reports.length ? 'Coba ubah kata kunci atau filter yang dipilih.' : 'Impor Markdown atau tulis laporan harian pertama Anda.'} action={!reports.length && <Link to="/reports/new"><Button>Mulai menulis</Button></Link>} />}
  </motion.div>
}
